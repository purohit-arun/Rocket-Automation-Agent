{
  "name": "rocket-automation-agent",
  "version": "1.0.0",
  "description": "Playwright automation agent for Rocket.new — generates, publishes, and analyzes applications automatically.",
  "scripts": {
    "start": "node run.js",
    "build": "tsc",
    "test": "npx playwright test",
    "automation": "npx playwright test tests/runAutomation.spec.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "openai": "^4.78.1",
    "csv-parser": "^3.0.0",
    "csv-writer": "^1.6.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.1",
    "typescript": "^5.7.3",
    "ts-node": "^10.9.2",
    "@types/node": "^22.12.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}